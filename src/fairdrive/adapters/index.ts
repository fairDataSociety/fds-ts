/**
 * Storage Adapters
 *
 * Different implementations of the Fairdrive interfaces.
 */

// No adapters currently - @fairdrive/core provides direct Bee integration
// fdp-storage adapter removed (thin layer approach)

// Future adapters (if needed):
// export * from './fairos-dfs.js';    // Server-based adapter

// Export empty object to make this a valid module
export {};
